#include <stdio.h>
#include <stdlib.h>

 /*
	simple singly linked list, unordered, duplicates not allowed.
*/

// this enables us to just say "node" instead of "struct node"
typedef struct node{
	int key;
    int data;
    struct node * next;
} node;

int main(){

    return 0;
}
