# Makefile main.c shell.c shell.h
debbuger -> cdtdebug -e shell &
the & throughs this into the backgroud

#make file

shell: main.o shell.o
		$(CXX) $(CXXFLAGS) -o shell $^
		
main.o : main.c

shell.o : shell.c

clean :
		rm*~ *.o